<% @users.each do |user| %>
  <div style="padding: 10px 0;">
    <h3><%= user.name %></h3>
    <p><%= user.email %></p>
    <%= link_to "Send friends request",
             invitations_path(invitation: {from_user_id: current_user.id, to_user_id: user.id}), method: "post" %>
  </div>
<% end %>
